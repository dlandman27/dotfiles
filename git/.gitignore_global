# Global gitignore — applies to every repo via core.excludesfile.
# Keep this for OS/editor/tooling cruft only; project-specific ignores belong
# in each repo's own .gitignore.

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*

# Editors / IDEs
.idea/
.vscode/
*.swp
*.swo
*~

# Logs & temp
*.log
*.tmp

# Env / secrets (belt-and-suspenders; still prefer per-repo ignores)
.env.local
